{{#if hasBlock}}
  {{yield sanitizedValue}}
{{else}}
  {{#if raw}}
    {{! template-lint-disable no-triple-curlies }}
    {{{sanitizedValue}}}
    {{! template-lint-enable no-triple-curlies }}
  {{else}}
    {{sanitizedValue}}
  {{/if}}
{{/if}}
